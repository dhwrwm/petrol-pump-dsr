export type SaleSummary = {
  nozzle: string;
  fuel: string;
  liters: string;
  amount: string;
  method: string;
};

export type TankSummary = {
  name: string;
  fuel: string;
  level: number;
  stock: string;
};
